/**
 * Aurora component neutralized to preserve clean, unpolluted editorial backgrounds.
 */
export function Aurora({ variant: _variant = "default" }: { variant?: "default" | "calm" }) {
  return null;
}
